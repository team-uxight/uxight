# apps/web — UXight 관리자 웹 (React + TypeScript, Vite)

```sh
npm ci && npm run dev        # http://localhost:5173
npm run lint && npm run typecheck && npm run build
```

환경변수는 `.env.example` 참고 (`VITE_API_BASE_URL`).
