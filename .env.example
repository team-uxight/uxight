# compose 용. 값을 채워 .env 로 복사한다 — .env 는 커밋되지 않는다 (docs/secrets.md)
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=uxight
MYSQL_USER=uxight
MYSQL_PASSWORD=
# LLM (OpenAI-compatible 프록시) — PM 이 1인 1키 발급
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
